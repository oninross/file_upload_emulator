{
  "name": "file-upload-emulator",
  "version": "1.0.0",
  "description": "A file uploader emulator using JS and NodeJS",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.13.3"
  }
}
